REM This command can be run to change the boot timeout delay that allows for OS selection. This
REM can be useful on a Windows Server Core server that does not have the GUI to update.
REM The following command sets OS boot timeout to 5 seconds.

bcdedit /timeout 5